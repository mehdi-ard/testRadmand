import style from 'provider/style';

const styles = (theme) => ({
  root: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
    overflow: 'auto',
    flexWrap: 'wrap',
  },
});

export default style(styles);
