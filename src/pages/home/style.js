import style from 'provider/style';

const styles = (theme) => ({
  root: {
    padding: [0, 15],
    maxWidth: 1280,
    margin: '0 auto',
  },
  cardList: {
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
  },
});

export default style(styles);
